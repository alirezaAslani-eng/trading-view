"use client";
import { styled } from "@mui/material";
import { NumericFormat as NumericFormat_ } from "react-number-format";

const NumericFormat = styled(NumericFormat_)();

export default NumericFormat;
