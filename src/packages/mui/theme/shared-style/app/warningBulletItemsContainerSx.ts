import { SxPropOnlyObject } from "@/packages/mui/theme/types";

const warningBulletItemsContainerSx: SxPropOnlyObject = {
  display: "flex",
  flexDirection: "column",
  gap: "20px", // * space between bullet items
  mt: "32px",
};

export default warningBulletItemsContainerSx;
