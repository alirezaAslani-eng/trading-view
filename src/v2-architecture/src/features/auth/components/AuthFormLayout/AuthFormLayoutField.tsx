import { Stack, StackProps } from "@mui/material";

function AuthFormLayoutField(props: StackProps) {
  return <Stack spacing={{ xs: 2, sm: 2.5 }} {...props} />;
}

export default AuthFormLayoutField;
